Nov 9, 2013:
+ replaced github.com/kless/goconfig which doesn't exist with 
  github.com/msbranco/goconfig, required minor tweaking.
+ Migrated SQL schema to etc
+ fixed several problems in example config file, it's now more generic
  and should work out of the box
+ add server port to [server] section of config and added support for it
+ fixed README to make running a simple example cut and paste.
+ added support for per blob return values to support:
  1 = HAVE: already have blob, you can subscribe
  2 = ACCEPT: will accept an upload blob
  3 = REJECT: will not accept upload blob


 
